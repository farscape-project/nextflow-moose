executor {
  // Use the LSF executor
  name = "lsf"

  // Set the default maximum number of submitted jobs to 100
  queueSize = 20
}

process {
    queue = "SKL"
    time = { 1.hour }
    cpus = 32
}

